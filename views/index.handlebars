<h1>Burgers!</h1>

<h2>Burgers that have not yet been eaten!</h2>

<ul>
  {{#each burgerss}}
    {{#unless eaten}}
      {{> burgers/burger-block eaten=false}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have been eaten!</h2>

<ul>
  {{#each bugers}}
    {{#if eaten}}
      {{> burgers/burger-block eaten=true}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burgers_name">Burger Type:</label>
    <input type="text" id="burgers_name" name="name">
  </div>

  <div class="form-group">
    <label for="Eat">Eaten?</label><br>
    <input type="radio" name="Eaten" value="1" checked> Eaten!<br>
    <input type="radio" name="Not eaten" value="0"> Not yet eaten
  </div>

  <button type="submit">Add Burger</button>
</form>
